const Employee = require("./Employee")

class Engineer extends Employee {
    constructor(name, ID, email, GithubUserName) {
    newEmployee.call(name, ID, email)
    this.GithubUserName = GithubUserName
    }
}